# pratik@goku-py:~$

> This profile has 0 badges, 0 stats cards, 0 typing animations, 0 "passionate developer" lines.
> They all look the same and half of them break. My repos are the stats.

```
$ whoami --short
pratik — backend-leaning full-stack. I build things that survive reboot.
python / fastapi / typescript / postgres / docker. The boring stack, used aggressively.
```

---

```
$ ls ~/builds --sort-by="actually-shipped"
```

**ai-mentor** → an AI study mentor that's live, not a screenshot.
`python / ai / backend` — [repo](https://github.com/Goku-py/AI-Mentor) / [live](https://ai-mentor.up.railway.app/)

**specwise** → paste a messy spec, get structured output.
`typescript / next.js` — [repo](https://github.com/Goku-py/SpecWise) / [live](https://spec-wise-three.vercel.app)

**port-scanner-gui** → TCP port scanner with live progress + export. No nmap install needed.
`python / tcp-ip / tkinter` — [repo](https://github.com/Goku-py/Port-Scanner-GUI)

**portable-ai** → AI off a pendrive. No internet, no cloud, no excuses.
`offline / systems` — [repo](https://github.com/Goku-py/Portable-AI)

**portfolio** → pratikworks. Where I pretend to like CSS.
`typescript / react` — [repo](https://github.com/Goku-py/Portfolio) / [live](https://pratikworks.vercel.app)

**8icon** → gen-z e-commerce experiment. Fast, visual, loud.
`full-stack` — [repo](https://github.com/Goku-py/8icon)

---

```
$ cat ./opinions.md
```

1. If it only runs on your machine, it doesn't run. Ship a Dockerfile or it didn't happen.
2. Slow is a bug. Latency is the only benchmark users actually feel.
3. CI/CD > heroics. If deploy needs courage, the pipeline is broken.
4. I automate anything I do twice. The third time should never come.

---

```
$ sudo sysctl -a | grep -i latency   # the weird corner of this profile
```

Most dev profiles say "ask me about React." Mine says this:

- **Windows gaming optimization** — power plans, HAGS, interrupt affinity, killing the 47 background things eating your frame time.
- **TCP/IP + networking** — ports, sockets, why your "fast" API waits 400ms on DNS.
- **System latency tuning** — measuring before guessing. `ping` > opinions.

Currently going deep on: system design, distributed systems, k8s, Go, backend perf.
Currently building: an AI code-execution platform with Docker-isolated runtimes.

---

```
$ tail -n 3 ./now.log
open to: backend infra, devtools, AI engineering collabs.
need help with: distributed systems at real scale. will trade code review for war stories.
reach me: [pratikworks.vercel.app](https://pratikworks.vercel.app) / [propratik087@gmail.com](mailto:propratik087@gmail.com)
```

```
// end of transmission — scouter reads: power level stable, latency low.
```
